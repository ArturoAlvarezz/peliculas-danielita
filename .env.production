# Variables de entorno para producción

# URL del backend API (sin /api al final, se agrega en el código)
VITE_API_URL=https://backend-peliculas.arturoalvarez.site

# Puerto del servidor backend
PORT=3000

# Entorno
NODE_ENV=production
